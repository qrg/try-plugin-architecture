{
  "name": "try-plugin-architecture",
  "version": "0.0.1",
  "description": "* clients   * browser extension   * app * each client has modules as plugin",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp build",
    "watch": "gulp watch",
    "clean-install": "rm -rf ./node_modules && npm cache clean && npm cache ls && rm -rf ~/.npm && npm install"
  },
  "author": "qrg",
  "license": "MIT",
  "dependencies": {
    "whatwg-fetch": "^1.0.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "del": "^2.2.2",
    "eslint": "^3.3.1",
    "glob": "^7.0.5",
    "gulp": "^3.9.1",
    "gulp-if": "^2.0.1",
    "gulp-tap": "^0.1.3",
    "gulp-util": "^3.0.7",
    "gulp-watch": "^4.3.9",
    "node-static": "^0.7.8",
    "pretty-time": "^0.2.0",
    "run-sequence": "^1.2.2",
    "sinon": "^1.17.5",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0",
    "watchify": "^3.7.0"
  }
}
